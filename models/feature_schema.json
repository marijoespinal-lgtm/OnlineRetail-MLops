{
  "model_version": "5",
  "feature_set": "RFM",
  "required_input_columns": [
    "recency",
    "frequency",
    "monetary"
  ],
  "note": "El input debe traer estas columnas SIN escalar. La API debe cargar customer_segmentation_bundle.pkl y usar bundle['scaler'] y bundle['feature_columns_scaled'] para transformar antes de llamar a bundle['model'].predict()."
}